# Finite-Candidate CIC Recovery Theory

This document states the conditional, finite-candidate theory behind the CIC
shortcut-repair results and records whether its central assumption was
**empirically validated** for the OpenCLIP benchmarks in this repository.

The theory is deliberately narrow. It is a **conditional, finite-candidate**
account of when neutralizing a shortcut region restores the causal prediction.
It does **not** prove open-world shortcut discovery, exact bounding-box
localization, or general robustness. See [Scope](#8-scope) below.

Whether the theory's additive-channel assumption actually holds for CLIP is an
empirical question, answered by the embedding-additivity validation experiment
(`causal_reliability/experiments/run_embedding_additivity_validation.py`,
results in `results/embedding_additivity/`). The decisive booleans are
`embedding_additivity_supported_for_text` and
`embedding_additivity_supported_for_watermark`; the
[empirical-support section](#empirical-support-status) records the outcome.

---

## 1. Setup

A labelled input is generated by a structural process

    X = g(C, S, N),   Y = h(C)

where

- `C` is the **causal content** (the true object/shape that defines the label),
- `S` is a **shortcut factor** (a spurious feature correlated with the label in
  the data but not label-defining — e.g. a misleading text overlay or a
  colored-symbol watermark),
- `N` is **nuisance** variation (placement jitter, decoys, background),
- `h` is the true labelling function, depending only on `C`.

A classifier predicts `argmax_y f_y(X)` from per-class scores `f_y(X)` (logits).
The shortcut value ranges over a **finite candidate set** `S in {s_0, s_1, ...}`
including a designated **neutral value** `s_0` (no harmful shortcut).

## 2. Additive-channel assumption

The theory assumes the per-class logit decomposes additively into a causal
channel, a shortcut channel, and an interaction residual:

    logit_y(g(C, S, N)) = phi_y(C, N) + psi_y(S) + xi_y(C, S, N)

- `phi_y(C, N)` — the **causal channel** (depends on content and nuisance, not on
  the shortcut),
- `psi_y(S)` — the **shortcut channel** (depends only on the shortcut value),
- `xi_y(C, S, N)` — a small **interaction residual**.

### Why this is plausible for CLIP

For zero-shot CLIP, logits are inner products

    logit_y(X) = <u(X), v_y>

where `u(X)` is the (L2-normalized) image embedding and `v_y` is the class text
embedding. Hence the logit is additive in the embedding exactly when the
embedding is additive. The shortcut channel `psi_y(S) = <delta_S, v_y>` is well
defined with an approximately **input-independent shortcut direction** `delta_S`
iff

    u(object + shortcut) - u(object only)  ~=  delta_S      (independent of the object)

So the additive-channel assumption for CLIP reduces to the empirical claim that
the embedding shift caused by a shortcut clusters by **shortcut value**, not by
object. This is exactly what the embedding-additivity validation measures.

## 3. Neutralization recovery theorem (exact)

**Theorem (exact neutralization).** Suppose the additive decomposition holds with
`xi_y ≡ 0`, and the neutral shortcut value `s_0` makes the shortcut channel
**class-independent**, i.e. `psi_y(s_0) = c` for all `y` (a constant offset).
Then for any input the argmax over `y` of `phi_y(C, N) + psi_y(s_0)` equals the
argmax of `phi_y(C, N)`. Therefore replacing the observed shortcut by `s_0`
(exact neutralization) recovers the **causal argmax** `argmax_y phi_y(C, N)`,
which equals the prediction the model would make on the shortcut-free input.

*Intuition.* A class-independent shortcut offset cannot change which class wins;
removing/neutralizing the shortcut removes the only term that could.

## 4. Approximate recovery theorem (margin condition)

Exact neutralization and `xi_y ≡ 0` never hold perfectly. Let the causal margin
of an input be

    m(X) = phi_{y*}(C, N) - max_{y != y*} phi_y(C, N),   y* = argmax_y phi_y(C, N)

and bound the residual errors by `epsilon_C` (causal-channel damage from imperfect
neutralization), `epsilon_S` (residual shortcut channel after neutralization), and
`epsilon_xi` (interaction residual).

**Theorem (approximate recovery).** If

    m(X) > 2 (epsilon_C + epsilon_S + epsilon_xi)

then the argmax after neutralization equals the causal argmax `y*`.

*Intuition.* Each error term can move a class score by at most its bound; the
total worst-case swing between the winner and any runner-up is twice the summed
bounds. If the causal margin exceeds that swing, the winner cannot be overtaken.

The experiment uses an embedding-space proxy: `causal_margin_proxy` is the CLIP
margin on the no-overlay image, and `residual_error_proxy` is the maximum
per-class logit difference between the neutralized and no-overlay images. The
diagnostic `margin_condition_satisfied = causal_margin_proxy > 2 *
residual_error_proxy` is compared against actual repair success.

## 5. Consensus / class-balanced sweep theorem

Instead of a single neutral value, average the logits over a **class-balanced
sweep** of shortcut values `S in B`:

    bar_f_y(X) = (1/|B|) sum_{s in B} logit_y(g(C, s, N))
               = phi_y(C, N) + (1/|B|) sum_{s in B} psi_y(s) + (small interaction).

**Theorem (class-balanced sweep).** If the **average shortcut channel**
`(1/|B|) sum_{s in B} psi_y(s)` is class-independent (a constant in `y`), then
averaging logits over the class-balanced sweep cancels the shortcut contribution
up to a class-independent offset, and `argmax_y bar_f_y(X) = argmax_y phi_y(C,N)`.

This is the theoretical basis for consensus / sweep-style repair: a balanced
sweep cancels the shortcut even when no single value is perfectly neutral.

## 6. Coarse-localization interpretation

The recovery theorems depend on the **causal-channel damage** `epsilon_C` being
small — i.e. neutralization must not destroy the causal content — **not** on
exact bounding-box overlap. A coarse region that covers the shortcut while
leaving most of the object intact yields small `epsilon_C` and large residual
shortcut removal, which is sufficient for the margin condition.

This explains why repair can succeed at coarse IoU (e.g. IoU ≥ 0.3) while exact
localization (IoU ≥ 0.5) is weak: the theorem only needs low causal-channel
damage, so coarse localization suffices. It does **not** justify claiming precise
localization, and the repository does not claim it.

## 7. Negative-result interpretation

If the shortcut channel `psi_y(S)` is **flat or weak** — i.e. `psi_y(s)` varies
little across shortcut values, or `||delta_S||` is small — then there is little
shortcut contribution for neutralization to cancel. In that regime
neutralization cannot repair much, because the model's failure (if any) is not
driven by a strong, separable shortcut channel.

This is the mechanism behind the **colored-symbol watermark cross-shortcut
negative result**: if the watermark shortcut channel is weak/flat, the frozen
CIC policy has little to neutralize, and the transfer failure is *consistent with
the theory* rather than a refutation of it. The embedding-additivity validation
reports `watermark_shortcut_channel_weak` to test this directly.

## 8. Scope

This theory is **conditional and finite-candidate**:

- It assumes an additive logit decomposition with a designated finite candidate
  set of shortcut values including a neutral value.
- It explains recovery *given* a shortcut region/value; it does **not** prove
  open-world shortcut discovery.
- It depends on low causal-channel damage, not exact bounding-box IoU; it does
  **not** justify claiming exact localization.
- It is a statement about a controlled, finite-candidate setting; it does **not**
  prove general robustness.

## 9. Per-Input Class-Balance Condition

The embedding-additivity validation (Section 2 / Empirical-support status) tested a
**stronger-than-necessary** condition: a single global, input-independent shortcut
direction `delta_S` shared across all objects. That global additivity is
*sufficient* for the recovery theorem but is **not required** by it. The recovery
theorems above only need a **weaker per-input condition**.

Let `x_clean` be the no-overlay/clean image with the same causal content, and let
`T(x)` be the neutralized/repaired version of the misleading image. Define the
per-class **residual-to-clean**

\[
\rho_y(x) = \ell_y(T(x)) - \ell_y(x_{\mathrm{clean}})
\]

(equivalently `rho_y(x) = logit_y(T(x)) - logit_y(x_clean)`). Let its class mean be

\[
\bar{\rho}(x) = \frac{1}{|\mathcal{Y}|}\sum_y \rho_y(x).
\]

**Per-input residual-to-clean class-balance condition.** The repaired logits differ
from the clean logits by an approximately class-independent residual for the input
`x` when the maximum class-centered deviation is bounded by `epsilon_B`:

\[
\max_y |\rho_y(x) - \bar{\rho}(x)| \leq \epsilon_B.
\]

The residual is defined relative to the clean/causal logits, **not** relative to the
misleading input logits: a shift that is class-balanced relative to the misleading
logits would preserve the misleading argmax, whereas recovery requires returning to
the clean causal argmax. This condition is **weaker** than global embedding
additivity. It allows the shortcut effect to vary across inputs (it does **not**
require one shared `delta_S` for all objects), as long as the repaired logits stay
close to the clean logits up to a class-independent offset *within a given input*.
Global additivity implies the per-input condition, but the converse is false, so the
per-input condition can hold even when the global condition fails — which is
exactly the regime observed for OpenCLIP text overlays.

### Corollary: Per-Input Residual-to-Clean Recovery

**Corollary.** If the repaired logits differ from the clean logits by an
approximately class-independent residual (i.e. `max_y |rho_y(x) - bar_rho(x)| <=
epsilon_B`), and the clean causal margin `m_clean(x)` satisfies

\[
m_{\mathrm{clean}}(x) > 2\epsilon_B,
\]

then the repaired argmax equals the clean causal argmax:
`argmax_y ell_y(T(x)) = argmax_y ell_y(x_clean)`.

*Proof.* Write the repaired logits as `ell_y(T(x)) = ell_y(x_clean) + bar_rho(x) +
r_y(x)` where the class-independent part `bar_rho(x)` is constant in `y` and the
residual `r_y(x) = rho_y(x) - bar_rho(x)` satisfies `|r_y(x)| <= epsilon_B`. The
common offset `bar_rho(x)` shifts every class equally and **does not change the
argmax**. The residual can change the ranking of the clean winner `y*` against any
runner-up `y` only if it closes their gap, and the worst-case swing is
`|r_{y*}(x)| + |r_y(x)| <= 2\epsilon_B`. If the clean causal margin exceeds
`2\epsilon_B`, no runner-up can overtake `y*`, so the repaired argmax equals the
clean causal argmax `y* = argmax_y ell_y(x_clean)`. ∎

The empirical validation uses residual-to-clean logit units directly: the per-input
residual is `residual = max_y |delta_to_clean_y - mean_y delta_to_clean|` where
`delta_to_clean_y = logit_y(x_neutralized) - logit_y(x_clean)`, the clean causal
margin proxy `m_clean(x)` is the top-1-minus-runner-up logit gap on the no-overlay
image, and `margin_condition_satisfied = m_clean(x) > 2 * residual` is compared
against actual repair success. The mechanism is validated most tightly for oracle
neutralization (median residual-to-clean ≈ 2.46, repair accuracy 1.000) and
approximately for CIC (median residual-to-clean ≈ 3.70 top-1, repair accuracy
0.781). Because CIC's median residual exceeds `epsilon_B = 3.0`, the theorem does
not fully explain every CIC success: CIC neutralization is more class-balanced than
matched random repair (median residual-to-clean ≈ 5.22, repair accuracy 0.406) and
aligns with the recovery condition directionally, but the worst-case margin
condition is conservative, and many successful CIC repairs occur even when the
sufficient condition is not formally satisfied.

### Object-Entangled Typographic Shortcut Effects

The embedding-additivity result is itself a **positive structural finding** about
how the OpenCLIP typographic shortcut behaves. The same-shortcut overlay deltas
cluster above the shuffled-label baseline (within-shortcut cosine ≈ 0.76 vs shuffled
≈ 0.63), confirming a real shortcut component — **but** the within-object cohesion
exceeds the within-shortcut cohesion (within-object cosine ≈ 0.86 > 0.76), so the
shift direction varies substantially with the underlying object.

> OpenCLIP's typographic shortcut effect is not a single global additive bias
> direction. The shift induced by overlay text is **object-entangled**: it contains
> a real shortcut component, but its direction varies substantially with the
> underlying object. This helps explain why generic global debiasing is unlikely to
> suffice, and why targeted per-input counterfactual region scoring can still repair
> failures.

Object-entanglement is why the global additivity condition fails while the weaker
per-input class-balance condition can still hold: the shortcut effect need not be a
shared global direction for *per-input* neutralization to leave an approximately
class-independent residual.

---

## Empirical-support status

The additive-channel assumption (Section 2) is an empirical claim about CLIP
embeddings. The embedding-additivity validation experiment tests it on the hard
multi-decoy **text-overlay** shortcut and the colored-symbol **watermark**
shortcut, using real pretrained OpenCLIP (`open_clip` / `ViT-B-32` /
`laion2b_s34b_b79k`; no fake backend for theory evidence).

The authoritative outcome is in
`results/embedding_additivity/embedding_additivity_key_numbers.json`:

- **Text overlays.** When `embedding_additivity_supported_for_text = true`, the
  shortcut embedding shift clusters by shortcut value above the shuffled-label
  baseline, shortcut-value clustering exceeds object clustering, neutralization
  damage is small relative to the shortcut effect, and the margin condition
  separates repair success from failure. In that case the finite-candidate
  recovery theorem is **empirically supported** as a mechanism explanation for
  the OpenCLIP text-repair result. When the flag is `false`, the theorem is
  presented as **conditional / theoretical only**, and is **not** claimed to
  apply directly to the OpenCLIP text-overlay benchmark.
- **Watermark.** `embedding_additivity_supported_for_watermark` records whether
  the same holds for the watermark. If it is `false` and
  `watermark_shortcut_channel_weak = true`, the cross-shortcut negative result is
  explained by a weak/flat shortcut channel, consistent with Section 7.

> **Validation outcome (this repository run).** On real pretrained OpenCLIP
> (`open_clip` / `ViT-B-32` / `laion2b_s34b_b79k`), the embedding-additivity
> validation **did not support the strict additive-channel assumption for the
> hard multi-decoy text-overlay shortcut** (`embedding_additivity_supported_for_text
> = false`). The shortcut embedding shift *does* cluster by misleading-word value
> above the shuffled-label baseline (within-shortcut cosine ≈ 0.76 vs shuffled
> ≈ 0.63), the logit decomposition is exactly additive in embedding space (CLIP
> logits are inner products; consistency MAE ≈ 6e-15), and oracle neutralization
> of the harmful word restores the correct prediction in 100% of cases. **But the
> per-image overlay delta clusters *more* tightly by object class (within-object
> cosine ≈ 0.86) than by shortcut value (≈ 0.76), so the shortcut direction is not
> input-independent**, and the neutralization L2 damage is not small relative to
> the multi-decoy shortcut effect (ratio ≈ 0.92, dominated by the remaining neutral
> decoys). Therefore the finite-candidate recovery theorem is presented here as
> **conditional / theoretical only** and is **not** claimed to apply directly to
> the OpenCLIP text-overlay repair result. The validation also **did not support
> additivity for the colored-symbol watermark**
> (`embedding_additivity_supported_for_watermark = false`); the watermark shortcut
> channel was weak/flat (within-object cosine ≈ 0.92 ≫ within-shortcut ≈ 0.76),
> which is consistent with — and explains — the cross-shortcut transfer negative
> result (Section 7). The precise numbers are recorded in
> `results/embedding_additivity/embedding_additivity_key_numbers.json` and
> `results/embedding_additivity/embedding_additivity_summary.md`, which are
> authoritative if they differ from this paragraph.

Mechanistically, this means the text-overlay repair result is better explained by
**finite-candidate region scoring** (the harmful region can be located and scored
among a finite proposal set, and neutralizing it restores the causal margin) than
by a single clean global input-independent shortcut direction. The recovery
theorem remains a valid *conditional* account — it states what would follow *if*
the additive-channel assumption held — but the **global** additivity validation
shows the strong assumption is only approximately and partially met on the current
CLIP benchmark.

### Per-input class-balance validation (final theory gate)

Because global additivity is stronger than the recovery theorems actually require
(Section 9), a final experiment tests the **weaker per-input class-balance premise**
directly:
`causal_reliability/experiments/run_per_input_class_balance_validation.py`, results
in `results/per_input_class_balance/`. For each misleading-overlay image it measures
whether oracle, CIC top-1, and CIC top-3 consensus neutralization produce an
approximately class-independent residual logit shift (small
`max_y |delta_to_clean_y - mean_y delta_to_clean|`), compares this against a matched
random text-region neutralization control, and checks whether the margin condition
`m(x) > 2 * residual` aligns with repair success.

The authoritative outcome is in
`results/per_input_class_balance/per_input_class_balance_key_numbers.json`:
`per_input_class_balance_supported_for_text` (`true` / `false` / `mixed`) and
`clip_theory_support_status` (one of "CLIP-supported via per-input class-balance",
"mixed; per-input class-balance partially supported", or "conditional only;
per-input class-balance not supported"). On real pretrained OpenCLIP, oracle and
CIC neutralization are substantially more class-balanced than matched random
neutralization, and the margin condition tracks repair success; the precise status
is recorded in that JSON and in `per_input_class_balance_summary.md`, which are
authoritative if they differ from this paragraph. This is the **final** theory gate;
the theory section is frozen after it. Whatever the status, the repository does
**not** claim open-world shortcut discovery, exact localization, or general
robustness, and it records the **object-entangled** character of the typographic
shortcut effect (Section 9) as the reason global debiasing is insufficient while
per-input neutralization can still repair failures.
