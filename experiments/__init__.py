"""Top-level experiment entrypoints for the automated-proposal CIC pilot.

These scripts are runnable directly (``python3 experiments/<file>.py``) and are
also importable (``from experiments.run_auto_proposal_cic_pilot import ...``) so
tests can exercise them. They write only under ``results/auto_proposal_pilot/``.
"""
